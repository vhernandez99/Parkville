<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Parkville.Views.Ubicacion"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="WhiteSmoke"
             xmlns:cards="clr-namespace:PanCardView;assembly=PanCardView" 
             xmlns:controls="clr-namespace:PanCardView.Controls;assembly=PanCardView">
    <ContentPage.Content>
        <ScrollView>
            <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="0.5*,0.5*"  >
                <Frame Padding="0" BackgroundColor="#BF0000" Grid.Row="0" Grid.ColumnSpan="2" Margin="-10">
                    <Label FontSize="Medium" Text="¿No sabes como llegar?" HorizontalTextAlignment="Center" TextColor="White" Padding="15"></Label>
                </Frame>
                
                <Frame Grid.Row="1" Padding="0" Grid.ColumnSpan="2" Margin="10">
                    <Image Aspect="AspectFill"  Source="mundo"/>
                </Frame>
                <!--<Button Grid.Row="3" Grid.Column="0" Text="Ver ubicacion" BackgroundColor="#4bb649" x:Name="ubicacion" Clicked="ubicacion_Clicked"></Button>-->
                <Button Grid.Row="3" Grid.ColumnSpan="2"  Text="Ver ruta" BackgroundColor="#4bb649" x:Name="ruta" Clicked="ruta_Clicked" Margin="10"/>




            </Grid>
            
        </ScrollView>
    </ContentPage.Content>
</ContentPage> 